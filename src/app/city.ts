export class City {
    constructor(id: string, name: string, country: string) {
        this.id = id;
        this.name = name;
        this.country = country;
    }

    id: string;
    name: string;
    country: string;
}
